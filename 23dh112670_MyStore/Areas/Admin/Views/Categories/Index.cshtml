@model IEnumerable<_23dh112670_MyStore.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="bg-success"> Danh mục sản phẩm</h2class>

<p>
    @Html.ActionLink("Thêm mới", "Create", null, new {@class="btn btn-primary"})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryName)
            Tên danh mục
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryName)
        </td>
        <td>
            @Html.ActionLink("cập nhật", "Edit", new { id=item.CategoryID }) |
            @Html.ActionLink("xem chi tiết", "Details", new { id=item.CategoryID }) |
            @Html.ActionLink("xóa", "delete", new { id=item.CategoryID })
        </td>
    </tr>
}

</table>
